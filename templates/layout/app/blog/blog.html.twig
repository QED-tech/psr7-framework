{% extends 'default.html.twig' %}

{% block content %}
    <h1>All posts</h1>

    {% for post in posts %}
        <li>
            <a href="{{ post.id }}">{{ post.title }}</a>
        </li>
    {% endfor %}
{% endblock %}